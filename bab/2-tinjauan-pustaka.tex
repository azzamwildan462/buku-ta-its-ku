\chapter{TINJAUAN PUSTAKA}
\label{chap:tinjauanpustaka}

% Ubah bagian-bagian berikut dengan isi dari tinjauan pustaka
\section{Hasil penelitian/perancangan terdahulu}
Metode Regresi Polinomial adalah sebuah metode pendekatan terhadap data-data yang telah disediakan sebelumnya. Hasil keluaran dari metode Regresi Polinomial ini berupa rumusan matematika berdasarkan data-data yang telah disediakan. Regresi polinomial dapat bekerja secara efisien meskipun dengan model yang non-linear 
\parencite{ref_regresi}. Rumus 2.1 adalah rumusan dari regresi polinomial dengan satu variabel bebas: 
\begin{equation}
  y = a_0 + a_1x + a_2x^2 + \ldots + a_nx^n
\end{equation}

Dimana \(y\) adalah variabel terikat, \(x\) adalah variabel bebas, dan \(a_0, a_1, a_2, \ldots, a_n\) adalah koefisien dari regresi polinomial. Kalibrasi yang telah ada sebelumnya, menggunakan metode regresi polinomial untuk mengkalibrasi data kamera omnivision dengan cara memasukkan data kamera omnivision ke dalam rumusan regresi polinomial. Data kamera yang dimasukkan adalah jarak pada frame kamera sebagai variabel X sedangkan jarak sebenarnya sebagai variabel Y. 

Dengan mendapatkan data jarak sebenarnya dari data kamera, maka data tersebut bisa digunakan untuk menghitung posisi bola pada lapangan. Hal itu dilakukan dengan cara menghitung koordinat polar dari data kamera lalu mengubah koordinat polar tersebut menjadi koordinat kartesian. Berikut adalah \textit{pseudocode} dari kalkulasi data metode regresi polinomial:
\begin{algorithm}
  \caption{Kalkulasi data metode regresi polinomial}\label{alg:calculate_coordinates}
  \begin{algorithmic}[1]
  \Procedure{CalculateCoordinatesAndAngles}{}
      \State \text{Compute } $dx \gets x - x\_center\_cam$
      \State \text{Compute } $dy \gets y\_center\_cam - y$
      \State \text{Compute } $\theta \gets \arctan(\frac{dy}{dx})$
      \State \text{Compute } $x\_world \gets jarak\_sebenarnya \times \cos(\theta)$
      \State \text{Compute } $y\_world \gets jarak\_sebenarnya \times \sin(\theta)$
  \EndProcedure
  \end{algorithmic}
\end{algorithm}

Dengan $x\_world$ yang merepresentasikan koordinat x pada lapangan dan $y\_world$ yang merepresentasikan koordinat y pada lapangan. Maka bisa didapat posisi objek pada lapangan.

\section{Teori/Konsep Dasar}

Ada beberapa teori dan konsep dasar yang digunakan dalam penelitian ini, diantaranya adalah:

\subsection{Kalibrasi}
\label{sec:kalibrasi}

Kalibrasi adalah sebuah proses untuk mengatur ulang parameter-parameter pada sebuah sistem agar sesuai dengan standar yang telah ditentukan sebelumnya. Kalibrasi banyak digunakan pada sistem pengukuran, sistem robotika, sistem otomasi, dan sistem lainnya. Kalibrasi pada kamera adalah sebuah proses untuk mengatur ulang kamera agar bisa melihat objek dengan jarak yang sesuai dengan jarak sebenarnya. Kalibrasi pada kamera biasanya dilakukan secara software dengan cara memasukkan data kamera ke dalam proses komputasi. Proses tersebut dapat mengubah data kamera sesuai dengan data sebenarnya. Rumus umum kalibrasi adalah sebagai berikut: 

\begin{equation}
  y = ax + b
\end{equation}

Dimana $y$ adalah informasi dunia yang diinginkan, $x$ adalah informasi yang didapat dari sensor, $a$ dan $b$ adalah parameter yang akan diubah oleh proses kalibrasi. Sehingga dengan menggunakan parameter-parameter yang telah terkalibrasi, maka data yang didapat dari sensor akan sesuai dengan data sebenarnya.

\subsection{Kamera \emph{Omnivision}}
\label{sec:omnivision}
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.20]{gambar/omnivisino2.jpeg}
    \caption{Kamera Omnivision. \Parencite{asd}}
    \label{fig:omnivision}
\end{figure}
Kamera \emph{omnivision} adalah sebuah kamera yang bisa melihat 360 derajat sekitar 
kamera tersebut \parencite{ref_kamera_omni}. 
Jarak pandang kamera \emph{omnivision} tidak terbatas 
tergantung dari resolusi kamera itu sendiri dan 
konstruksi cerminnya. Pada dasarnya kamera \emph{omnivision} 
adalah kamera biasa yang ditembakkan ke sebuah cermin cembung 
sehingga pandangan kamera tersebut bisa ke segala arah. Berikut adalah gambar konsep kamera \emph{omnivision}: 
\begin{figure}[H]
    \centering
    \includegraphics[scale=3]{gambar/omnivision3.jpg}
    \caption{Konsep Kamera Omnivsion. \Parencite{asd3}}
    \label{fig:omnivision3}
\end{figure}

Dapat dilihat pada gambar diatas, kamera \emph{omnivision} adalah sebuah kamera yang menghadap 90 derajat terhadap bidang datar dan ditembakkan ke cermin cembung. Cermin cembung tersebut akan memantulkan cahaya yang masuk ke kamera begitu juga sebaliknya. Kerja tersebut membuat kamera \emph{omnivision} bisa melihat ke segala arah. 


\subsection{Machine Learning}

\emph{Machine Learning} adalah sebuah cabang dari kecerdasan buatan yang terbagi menjadi dua macam yaitu \emph{Supervised Learning} dan \emph{Unsupervised Learning}. \emph{Supervised Learning} adalah metode pembelajaran mesin yang menggunakan data latih yang sudah diberikan label. Sedangkan \emph{Unsupervised Learning} adalah metode pembelajaran mesin yang menggunakan data latih yang tidak diberikan label. \emph{Machine Learning} banyak digunakan untuk mengklasifikasikan data, regresi, dan lain-lain. Salah satu metode \emph{Machine Learning} yang banyak digunakan adalah \emph{Neural Network}. 

\subsubsection{\emph{Neural Network}}
\label{sec:nn}
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.20]{gambar/nn.png}
    \caption{Neural Network. \Parencite{asd2}}
    \label{fig:nn}
\end{figure}
\emph{Neural network} merupakan bagian dari Pembelajaran Mesin. 
\emph{Neural network} diciptakan untuk mengatasi masalah ketidaklinearan 
pada sebuah model \parencite{ref_neural_network}. Pada dasarnya, 
\emph{Neural network} hanyalah sekumpulan \emph{Neuron} 
yang terhubung oleh sebuah \emph{Weight} dan \emph{Bias}. Selain 
\emph{Weight} dan \emph{Bias}, ada juga namanya \emph{forward propagation} 
menggunakan \emph{activation function} atau 
biasa disebut transfer function. Selain \emph{forward propagation},
 terdapat \emph{backward propagation} menggunakan \emph{loss function}. 

\subsubsection{\emph{Activation Function}}
\emph{Activation function} adalah sebuah fungsi yang digunakan untuk men-transfer 
data pada masing-masing layer pada \emph{Neural Network}. 
Dalam \emph{Neural Network}, \emph{Activation function} berperan sebagai 
\emph{forward propagation} yaitu perjalanan dari layer input menuju 
layer output. Beberapa \emph{activation Function} memiliki nilai 
saturasi biasanya bernilai 1 contohnya Sigmoid yang bernilai 
pada interval 0 sampai 1. Ada juga \emph{activation Function} lain yaitu 
tanh yang bernilai pada interval -1 sampai 1 \parencite{ref_activation_function}.

\subsubsection{\emph{Loss Function}}
\emph{loss function} adalah bagian dari \emph{Neural Network} 
yang bertujuan untuk memberikan umpan balik 
pada model tentang baik atau buruknya fase \emph{training}. 
\emph{loss function} pada \emph{Neural Network} bekerja pada jalur 
\emph{Backward Propagation} yaitu dari layer output menuju layer input. 
Ada beberapa macam \emph{loss function} salah satunya adalah MSE 
(\emph{Mean Squared Error}). MSE \emph{loss function} lebih baik digunakan pada 
data dengan nilai fluktuasi yang rendah \Parencite{ref_loss_function}. 

Selain \emph{loss function}, ada sebuah teori lagi yaitu Optimizer. 
Optimizer adalah sebuah algoritma yang bisa digunakan untuk menentukan 
\emph{learning rate} sistem training. \emph{Learning rate} pada 
\emph{Neural Network} digunakan untuk mengatur seberapa cepat model 
akan konvergen terhadap data-datanya. 

\subsection{\emph{Mobile Robot}}
\label{sec:mobile_robot}
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.10]{gambar/iris1.jpeg}
    \caption{Robot IRIS tampak depan.}
    \label{fig:mobile_robot}
\end{figure}
\emph{Mobile Robot} adalah sebuah robot yang didesain agar bisa bergerak 
atau berpindah tempat dengan mudah. Performa \emph{Mobile Robot} banyak 
dikhususkan pada sistem tracking, lokalisasi, dan algoritma. Ketiga 
hal tersebut berdasar pada kemampuan sensing yang baik \Parencite{ref_mobile_robot}. 
Pada umumnya, sensor yang digunakan adalah kamera baik itu kamera biasa 
maupun kamera \emph{omnivision}. Penggunaan kamera \emph{omnivision} dapat 
membuat robot melihat ke segala arah, Namun pre-processing datanya yang 
lebih sulit dibandingkan dengan kamera biasa. 
Pada penelitian ini, robot yang digunakan adalah robot IRIS yang mempunyai kamera \emph{omnivision}. Selain itu, robot IRIS juga dilengkapi dengan software yang digunakan untuk mengolah data dari kamera \emph{omnivision} tersebut. 

\begin{figure}[H]
  \centering
  \includegraphics[scale=0.18]{gambar/spek_robot2.jpg}
  \caption{Spesifikasi robot IRIS.}
  \label{fig:spekrobot}
\end{figure}

Pada gambar 2.5 adalah spesifikasi dari robot IRIS. Robot IRIS memiliki 4 komponen utama yaitu Kamera \emph{Omnivision}, mini-PC, sistem kontrol elektronik, dan motor penggerak. 
Adapun spesifikasi kamera yang digunakan adalah kamera logitech c922 dengan antarmuka berupa USB 3.0, sedangkan untuk mini-PC yang digunakan adalah intel NUC6i7KYK yang terpasang Linux Ubuntu, untuk sistem kontrol elektronik yang digunakan adalah STM32 yang telah dikustomisasi sesuai kebutuhan robot IRIS, dan untuk motor penggerak yang digunakan adalah AC Servo motor dan brushed DC motor PG.

Kamera \emph{Omnivision} digunakan untuk mendapatkan informasi citra dari sekitar robot. Mini-PC digunakan untuk mengolah data dari kamera \emph{Omnivision} dan mengirimkan perintah ke sistem kontrol elektronik. Sistem kontrol elektronik digunakan untuk mengatur motor penggerak robot. Motor penggerak digunakan untuk menggerakkan robot. 

Selain spesifikasi hardware, adapun spesifikasi software dari robot IRIS adalah sebagai berikut: 

\emph{OpenCV} adalah library open-source yang dikembangkan oleh 
Intel dengan bahasa pemrograman C/C++. 
\emph{OpenCV} menyediakan banyak algoritma yang berhubungan dengan Visi 
Komputer \Parencite{ref_opencv}. OpenCV banyak digunakan untuk deteksi 
objek baik itu berdasarkan warna, bentuk, ukuran, dan lain lain 
sesuai kebutuhan program. 

ROS atau \emph{Robot Operating System} adalah sebuah platform yang berdiri 
diatas 
Linux dan berguna untuk sinkronisasi bagian bagian dari 
robot \Parencite{ref_ros}. ROS banyak digunakan sebagai inti 
pemrosesan data dari sebuah robot mulai dari pemrosesan data sensor 
hingga menjadi data aktuator. ROS menyediakan konsep modular programming 
dengan metode publish/subscribe untuk IPC (\emph{Inter Process Communication}) nya. Selain memudahkan untuk 
transfer data antar proses, ROS juga menyediakan timer dengan scheduler 
default nya mengikuti Default Linux Scheduler yaitu Priority-based scheduler. 
Hal itu memungkinkan pengguna untuk mengatur prioritas masing-masing bagian 
dari robotnya. 

Websocket adalah jenis protokol komunikasi berbasis protokol TCP (\emph{Transmission Control Protocol}). 
Protokol websocket membuat kedua pengirim dan menerima untuk selalu 
membuka socket nya agar bisa saling komunikasi. 
Dibandingkan dengan HTTP (\emph{Hypertext Transfer Transfer Protocol}), 
protokol Websocket memiliki 
latensi yang lebih baik \Parencite{ref_websocket}. Aplikasi 
websocket banyak digunakan untuk aplikasi obrolan (chat), game online,
 aplikasi yang membutuhkan data \emph{realtime}, dan masih banyak lainnya. 

\subsection{\emph{Lookup Table}}
\label{sec:lut}

\emph{Lookup Table} adalah sebuah tabel yang digunakan untuk mencari nilai dari sebuah variabel. \emph{Lookup Table} banyak digunakan pada sistem kontrol, sistem pengolahan data, dan sistem lainnya. Pada penelitian ini, \emph{Lookup Table} digunakan untuk mencari nilai jarak sebenarnya dari data kamera. \emph{Lookup Table} ini dibuat berdasarkan data kalibrasi. 